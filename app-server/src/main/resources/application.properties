## Server Properties
server.port= 8080
server.compression.enabled=true

## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url = jdbc:mysql://localhost:3306/acmsacdb?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
spring.datasource.username = root
spring.datasource.password = root

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect
spring.jpa.hibernate.ddl-auto = update

## Hibernate Logging
logging.level.org.hibernate.SQL= DEBUG

# Initialize the datasource with available DDL and DML scripts
spring.datasource.initialization-mode=always

## Jackson Properties
spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS= false
spring.jackson.time-zone= UTC

## App Properties
app.jwtSecret= JWTSuperSecretKey
app.jwtExpirationInMs = 604800000

## SMTP Setup
spring.mail.host=172.18.0.1
spring.mail.port=25

## Create admin account
# WARNING - replace password using environment variable in production
app.admin.username = admin
app.admin.password = doofusqueen
app.admin.email = cheese@cake.com
app.admin.name = De Admin

## Create a dummy user for testing
# WARNING - replace password using environment variable in production
app.user.username = user
app.user.password = doofusqueen
app.user.email = user@example.com
app.user.name = De User
